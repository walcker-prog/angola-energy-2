{
  "name": "access-parser-server",
  "version": "1.0.0",
  "description": "Server to parse Microsoft Access files (.accdb, .mdb) up to 200 MB",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "mdb-reader": "^3.1.0",
    "cors": "^2.8.5"
  }
}
